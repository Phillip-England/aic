---
$path(".")
$jump(1203, 930)
$click()
$click()
$type("v", ["RIGHT_COMMAND"])
$type("x", ["RIGHT_COMMAND"])
$type("v", ["RIGHT_COMMAND"])
$jump(AIC_X_START, AIC_Y_START)
$click()
---
I also need a way to \$press("BACKSPACE") certain keys like "BACKSPACE" or "ENTER" these types of keys, please implement the \$press("SOME_KEY") that enables me to do these sort of key presses please. 
