---
$path("./pkg")
$path("main.go")
$jump(1203, 890)
$click()
---
okay we need to make sure that if the PROMPT section of ./ai/prompt.md is empty, then we will not interpret the file or execute any tokens or copy to clipboard. Do not run if the prompt section of prompt.md is empty please.