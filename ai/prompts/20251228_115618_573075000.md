---
$path("./src")
$path("main.go")
$jump(1203, 890)
\$click()
\$click()
\$type("v", ["RIGHT_COMMAND"])
\$type("x", ["RIGHT_COMMAND"])
\$type("v", ["RIGHT_COMMAND"])
\$sleep(3000)
\$press("ENTER")
\$click()
\$jump(AIC_X_START, AIC_Y_START)
\$click()
---
OK, if the prompt section of my prompt file is empty, not the content hex section the context section could be full, but if the prompt section is empty, please do not execute or interpret the prompt file. Just leave it alone if the prompt section is empty.
