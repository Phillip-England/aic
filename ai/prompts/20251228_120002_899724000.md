---
$path("./src")
$path("main.go")
$jump(1203, 890)
\$click()
\$click()
\$type("v", ["RIGHT_COMMAND"])
\$type("x", ["RIGHT_COMMAND"])
\$type("v", ["RIGHT_COMMAND"])
\$sleep(3000)
\$press("ENTER")
\$click()
\$jump(AIC_X_START, AIC_Y_START)
\$click()
---
okay right now out prompt.md file is still being interpreted even if the PROMPT section is empty. If the prompt section is empty, do not process the file and execute its tokens and copy prompt to clipbaod. I do not want that done.

and also keep in mind the prompt section is made with --- --- not === ===
